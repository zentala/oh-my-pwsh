#!/bin/sh
#
# oh-my-pwsh pre-commit hook
#
# This hook runs unit tests before allowing a commit.
# It helps catch breaking changes early.
#
# OPTIONAL: This hook is bypassable with --no-verify
#   git commit --no-verify -m "WIP: quick save"
#
# Installation:
#   ./scripts/Install-GitHooks.ps1
#

# Find the directory where this hook is located
HOOK_DIR="$(cd "$(dirname "$0")" && pwd)"

# Call the PowerShell script
pwsh.exe -NoProfile -ExecutionPolicy Bypass -File "$HOOK_DIR/pre-commit.ps1"
exit $?
